import { makeStyles } from "@material-ui/core";

export default makeStyles((theme) => ({
  root: {
    flexGrow: 1,
  },
  title: {
    flexGrow: 1,
  },
  mainContainer: {
    justifyContent: "center",
    marginTop: "50px",
  },
}));
